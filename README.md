# RUN BUILD RUNNER
## flutter pub run build_runner build
## flutter pub run build_runner watch --delete-conflicting-outputs

# Generate L10
## flutter clean;flutter gen-l10n --output-localization-file=strings.dart --output-class=Strings --verbose;flutter pub get